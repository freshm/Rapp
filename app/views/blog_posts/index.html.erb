<% if @blog_posts.any? %>
  <% @blog_posts.each do |post| %>
    <div class="content post">
      <div class="page-header">
        <h1><span class="fa-dot-circle-o"><%= post.title %></span></h1>
      </div>
      <p>
        <%= post.content %>
      </p>
    </div>
  <% end %><% else %> 
  <div class="row">
    <h1>Welcome to nothing</h1>
      <p>Post's will come soon... very soon..</p>
  </div>
<% end %>